# 1. Backend Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: board-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: board-backend
  template:
    metadata:
      labels:
        app: board-backend
    spec:
      containers:
        - name: board-backend
          image: cheongmyeong/board-app:latest
          ports:
            - containerPort: 8080
          # 🚩 아래 내용을 추가하세요
          readinessProbe:
            httpGet:
              path: /api/health  # 🚩 새로 만든 경로로 변경
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health  # 🚩 동일하게 변경
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
---
# 2. Backend Service
apiVersion: v1
kind: Service
metadata:
  name: board-app-service
  annotations:
    cloud.google.com/neg: '{"ingress":true}'
    # 🚩 이 줄을 반드시 추가해야 BackendConfig가 작동합니다!
    cloud.google.com/backend-config: '{"default": "board-backend-config"}'
spec:
  type: ClusterIP # 프론트엔드만 백엔드에 접근하면 되므로 내부용으로 설정
  selector:
    app: board-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
# 3. Frontend Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: board-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: board-frontend
  template:
    metadata:
      labels:
        app: board-frontend
    spec:
      containers:
        - name: board-frontend
          image: cheongmyeong/board-frontend:latest
          imagePullPolicy: Always # 🚩 항상 새로운 이미지를 Pull 하도록 설정
          ports:
            - containerPort: 80
---
# 4. Frontend Service (사용자가 접속하는 대문 역할을 Ingress에게 넘깁니다)
apiVersion: v1
kind: Service
metadata:
  name: board-frontend-service
spec:
  type: ClusterIP  # 🚩 LoadBalancer에서 ClusterIP로 반드시 변경해야 합니다!
  selector:
    app: board-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
