apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: board-backend-config
spec:
  healthCheck:
    checkIntervalSec: 10
    timeoutSec: 5
    healthyThreshold: 2
    unhealthyThreshold: 3
    type: HTTP
    requestPath: /api/health  # 🚩 로드밸런서가 여기를 찌르도록 강제 지정합니다.
    port: 8080
