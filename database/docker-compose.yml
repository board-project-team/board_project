services:
  db:
    image: postgres:16
    container_name: board-postgres
    environment:
      POSTGRES_DB: board
      POSTGRES_USER: board_user
      POSTGRES_PASSWORD: board_pw
    ports:
      - "5432:5432"
    volumes:
      - board_pg_data:/var/lib/postgresql/data

volumes:
  board_pg_data: